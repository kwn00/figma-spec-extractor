## Example output

A finished spec, so the shape is concrete. **Read this only if the template in SKILL.md leaves you unsure** — it is not part of the workflow.

Two things to take from it, beyond the layout:

1. **The prose is Korean because the request was Korean.** That is the rule from SKILL.md § Language, not a property of this file. An English request produces this same document in English, headings included. The English template in SKILL.md and this Korean one are the same spec in two languages — nothing about either one is the default.
2. **The design's own text is untouched in both.** `회선번호`, `[다음]`, `5G 시그니처` stay as they are in the file, whatever language the sentences around them are in.
3. **Questions are deduplicated across screens.** Three screens each missing a loading state produce one entry listing all three — not three entries. The screen-specific failure right below it stays separate, because it has a different answer.

The Needs Answer section is the long one here. That is deliberate, and typical.

**SKILL.md holds the canonical template.** This file is one filled-in instance of it, kept here to make the shape concrete. If the two ever disagree, SKILL.md wins — and the disagreement is a bug worth fixing.

---

# 회선 해지

> 출처: https://figma.com/design/abc123/service?node-id=45-678 · 2026-07-30 추출 · 프레임 4개
> ⚠️ 피그마에서 자동 추출. 그려진 것만 담겨 있고, 의도된 것은 담겨 있지 않다.

## 개요

가입된 회선을 사용자가 직접 해지하는 플로우. 해지 대상 회선을 고르고, 약관에 동의하고, 사유를 선택하면 완료된다. 위약금과 미납 잔액이 약관 화면에서 함께 안내된다.

## 플로우

```
마이페이지(외부, 미포함)
  → 1. 회선 선택
  → 2. 해지 약관
  → 3. 해지 사유 선택
  → 4. 해지 완료
```

분기 없음. 되돌아가는 경로도 그려져 있지 않다 — 각 화면 좌상단에 `←` 아이콘은 있으나 연결된 프로토타입 링크가 없다.

## 화면

### 1. 해지_01_회선선택 `45:678`

**목적**: 보유 회선 중 해지할 회선을 하나 고른다.
**노출 조건**: 정의 안 됨 — 마이페이지 진입 경로만 있고 자격 조건은 어디에도 없다

**표시 데이터**

| 필드 | 예시 값 | 비고 |
|---|---|---|
| 회선번호 | 010-1234-5678 | |
| 요금제명 | 5G 시그니처 | |
| 가입일 | 2023.04.15 | |

**동작**

| 요소 | 동작 | 다음 화면 |
|---|---|---|
| 회선 카드 | 선택 상태로 전환 | — |
| [다음] | 약관 화면으로 이동 | 화면 2 |
| `←` | 정의 안 됨 | ? |

회선 카드는 3개 반복되며 구조는 동일하다.

**정의된 상태**: default, 카드 선택됨
**정의되지 않은 상태**: loading, 회선 0개, error

### 2. 해지_02_약관 `45:912`

**목적**: 해지 조건과 정산 금액을 안내하고 동의를 받는다.
**노출 조건**: 정의 안 됨

**표시 데이터**

| 필드 | 예시 값 | 비고 |
|---|---|---|
| 위약금 | 42,000원 | |
| 미납요금 | 0원 | |
| 잔여 할부금 | 187,200원 | |
| 정산 합계 | 229,200원 | |

**동작**

| 요소 | 동작 | 다음 화면 |
|---|---|---|
| 동의 체크박스 | 체크 시 [다음] 활성화 | — |
| [약관 전문 보기] | 정의 안 됨 (연결된 프레임 없음) | 이 기능을 벗어남 — ? |
| [다음] | 사유 선택으로 이동 | 화면 3 |

**정의된 상태**: [다음] 비활성 / 활성
**정의되지 않은 상태**: loading, error, 위약금 조회 실패

### 3. 해지_03_사유 `45:1104`

**목적**: 해지 사유를 수집한다.
**노출 조건**: 정의 안 됨

**표시 데이터**

| 필드 | 예시 값 | 비고 |
|---|---|---|
| 사유 목록 | 요금 부담 / 서비스 불만 / 타사 이동 / 기타 | 4개 고정 |
| 기타 입력란 | 내용을 입력해 주세요 | placeholder, 더미 |

**동작**

| 요소 | 동작 | 다음 화면 |
|---|---|---|
| 사유 라디오 | 단일 선택 | — |
| [해지 신청] | 완료 화면으로 이동 | 화면 4 |

"기타" 선택 시에만 입력란이 나타나는 것으로 보이나, 두 상태가 별도 프레임이 아니라 한 프레임의 숨김 레이어로 되어 있어 확실하지 않다.

**정의된 상태**: default
**정의되지 않은 상태**: loading, 제출 중, error, 미선택 상태로 제출 시도

### 4. 해지_04_완료 `45:1288`

**목적**: 해지 접수 완료를 알린다.
**노출 조건**: 정의 안 됨

**표시 데이터**

| 필드 | 예시 값 | 비고 |
|---|---|---|
| 해지 예정일 | 2026.08.31 | 더미로 추정 |
| 접수번호 | T-20260730-0417 | 더미로 추정 |

**동작**

| 요소 | 동작 | 다음 화면 |
|---|---|---|
| [확인] | 정의 안 됨 | ? |

**정의된 상태**: default
**정의되지 않은 상태**: 없음 (완료 화면 특성상 해당 없음)

## 누가 무엇을 보나

조건 단위로 한 줄. 이 파일은 조건 분기를 그리지 않았다.

| 조건 | 화면 | 무엇이 다른가 |
|---|---|---|
| 정의 안 됨 | 1, 2, 3, 4 | — |

## 용어

디자인이 쓰는 도메인 용어. **정의는 추출하지 않는다** — 파일에 정의가 없으면 확인 필요로 넘긴다.

| 용어 | 등장 화면 | 정의 |
|---|---|---|
| 위약금 | 2 | 정의 안 됨 — 확인 필요 |
| 잔여 할부금 | 2 | 정의 안 됨 — 확인 필요 |
| 정산 합계 | 2 | 정의 안 됨 — 확인 필요 |
| 해지 예정일 | 4 | 정의 안 됨 — 확인 필요 |

## 데이터 요구사항

전 화면에서 표시되는 필드, 중복 제거. 백엔드 대조용 목록.

피그마는 **표시된 문자열만** 보여준다. `42,000원`은 표기이고 그 뒤의 값이 아니다. 화면이 계산에 쓰는 값은 원시값이 따로 있어야 한다.

| 필드 | 화면 표기 | 원시값 필요 | 화면 | 비고 |
|---|---|---|---|---|
| 회선번호 | 010-1234-5678 | — | 1 | |
| 요금제명 | 5G 시그니처 | — | 1 | |
| 가입일 | 2023.04.15 | 예 — 날짜 연산 | 1 | 형식·시간대 미정의 |
| 위약금 | 42,000원 | 예 — 합산 대상 | 2 | 계산식 미확인 |
| 미납요금 | 0원 | 예 — 합산 대상 | 2 | |
| 잔여 할부금 | 187,200원 | 예 — 합산 대상 | 2 | |
| 정산 합계 | 229,200원 | 예 — 위 3개의 합 | 2 | 클라이언트 합산인지 서버 계산인지 미확인 |
| 해지 사유 코드 | 요금 부담 외 3종 | 예 — 코드값 | 3 | 라벨만 있고 코드 체계 미확인 |
| 해지 사유 기타 텍스트 | 내용을 입력해 주세요 | — | 3 | placeholder |
| 해지 예정일 | 2026.08.31 | 예 — 날짜 연산 | 4 | 더미로 추정 |
| 접수번호 | T-20260730-0417 | — | 4 | 더미로 추정 |

## 문구

사용자에게 보이는 문장, 중복 제거. 문구 검수용 목록.

| 문구 | 화면 | 비고 |
|---|---|---|
| 내용을 입력해 주세요 | 3 | placeholder, 더미로 추정 |

이 파일에는 안내·경고·에러 문구가 그려져 있지 않다. 빈 것 자체가 정보다 — 해지처럼 되돌릴 수 없는 플로우에 경고 문구가 하나도 없다.

## 이동 경로

액션이 사용자를 보내는 곳, 중복 제거.

| 목적지 | 출발 | 이 기능을 벗어나나 |
|---|---|---|
| 화면 2 | 화면 1 [다음] | 아니오 |
| 화면 3 | 화면 2 [다음] | 아니오 |
| 화면 4 | 화면 3 [해지 신청] | 아니오 |
| 정의 안 됨 | 화면 1·2·3·4 `←`, 화면 4 [확인] | ? |
| 정의 안 됨 | 화면 2 [약관 전문 보기] | ? — 외부 링크일 수 있다 |

## 확인 필요

기획자·디자이너에게 물어야 할 것. 개발 착수 전에 답이 나와야 하는 순서로 정렬.

### 🔴 블로커 — 답 없이는 만들 수 없다

- [ ] 화면 2: 위약금·잔여 할부금은 어느 API에서 오나. 정산 합계는 클라이언트 합산인가 서버 계산인가. 세 값의 원시 타입과 단위도 함께 — 화면에는 `42,000원` 표기만 있어 합산도 비교도 할 수 없다
- [ ] 화면 2: 미납요금이 0원이 아닐 때도 해지가 진행되나. 막힌다면 어느 화면에서 막나
- [ ] 화면 3: 사유 4종의 코드값. 화면에는 라벨만 있다
- [ ] 화면 4: 해지 예정일 산정 규칙. 즉시인가, 청구월 말일인가, 신청일 기준 N일 후인가
- [ ] 전체: 이 플로우에 진입할 수 있는 자격. 네 화면 어디에도 조건이 없다 — 준회원·법인·일시정지 회선도 들어오나
- [ ] 화면 2: `위약금`, `잔여 할부금`, `정산 합계`가 각각 무엇을 뜻하는가. 세 값의 관계가 정의되지 않아 이름만으로는 합산 규칙을 알 수 없다
- [ ] 전체: 해지에 본인인증이 필요한가. 필요하다면 어느 시점인가 — 플로우 어디에도 없다

### 🟡 엣지 케이스 — 구현하면 반드시 만난다

- [ ] 화면 1, 2, 3: 로딩 상태가 어디에도 그려져 있지 않다. 전체 스켈레톤인가 버튼 스피너인가, 세 화면이 같은 처리인가
- [ ] 화면 1, 2, 3: 요청 실패 시 화면이 정의되지 않았다. 공통 에러 처리로 묶이는가, 화면별로 다른가
- [ ] 화면 1: 회선 카드 컴포넌트에 `Disabled` variant가 있으나 이 화면에서 쓰이는지 확인하지 못했다. 해지 불가 회선을 표시하는 용도인가
- [ ] 화면 1: 보유 회선이 0개일 때. 진입 자체를 막는지, 빈 화면을 보여주는지
- [ ] 화면 1: 회선이 10개 이상일 때 스크롤인지 페이징인지. 상한이 있나
- [ ] 화면 2: 위약금 조회가 실패하면 무엇을 보여주나. 조회 없이 진행 가능한가 (위 공통 에러 항목과 답이 다를 수 있어 분리)
- [ ] 화면 3: 사유 미선택 상태에서 [해지 신청]을 누르면. 버튼 비활성인가 에러 메시지인가
- [ ] 화면 3: "기타" 입력란이 필수인가. 글자 수 제한은
- [ ] 화면 3: 제출 중 버튼 상태와 중복 제출 방지
- [ ] 전체: 중간 이탈 후 재진입 시 어디서 시작하나. 화면 1인가 이탈 지점인가
- [ ] 전체: `←` 뒤로가기 동작. 특히 화면 4에서 뒤로가면 어디로 가나

### 🟢 확인하면 좋은 것

- [ ] 화면 1: 요금제명이 길 때 말줄임인가 줄바꿈인가
- [ ] 화면 2: [약관 전문 보기]가 어디로 가나. 별도 화면인가 바텀시트인가 외부 링크인가
- [ ] 화면 4: [확인]을 누르면 어디로 가나. 마이페이지인가 홈인가
- [ ] 전체: 해지 신청을 취소·철회할 수 있나. 가능하다면 언제까지
- [ ] 전체: 다크 모드 대응 여부. 라이트 모드 프레임만 있다

## 추출 노트

- 4개 프레임 모두 `해지_NN_이름` 규칙을 따라 그룹핑 근거는 명확하다. 캔버스 배치도 좌→우 한 줄로 순서와 일치한다
- 프로토타입 링크는 [다음] 계열 버튼에만 걸려 있다. `←`, [확인], [약관 전문 보기]는 연결이 없어 "정의 안 됨"으로 적었다. 링크를 안 건 것인지 동작이 미정인 것인지는 파일에서 구분되지 않는다
- 화면 1의 회선 카드는 `Disabled` variant를 가진 컴포넌트의 인스턴스다. 이 화면의 인스턴스 3개는 모두 기본 variant라 해당 상태가 이 플로우에서 쓰이는지 파일만으로는 판단되지 않아, 정의된 상태로 적지 않고 확인 필요로 올렸다
- 화면 3의 기타 입력란은 숨김 레이어로 존재한다. 조건부 노출로 보이지만 조건이 명시된 곳은 없다
- 화면 4의 해지 예정일·접수번호는 더미로 추정했다. 형식이 그럴듯하나 다른 화면과 대조할 값이 없어 확정하지 못했다
- 위약금 42,000원은 실제 값일 수도, 더미일 수도 있다. 요금제(5G 시그니처)와의 관계를 파일 안에서 확인할 방법이 없었다
